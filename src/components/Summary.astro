---
interface Props {
	title: string;
  date: string;
	summary: string;
	href: string;
}

const { title, date, summary, href } = Astro.props;
---

<a href={href}>
	<h3>
     {date} - {title}
	</h3>
</a>
 <p>
  {summary}
 </p>
